# Kepler's Horizon Configuration
# Reload in-game with: configure reload conf

# Event Probabilities (0-100)
event.navigation_hazard = 15
event.resource_bonus = 10
event.anomaly_discovery = 5

# GameDev Overrides (0 = disabled)
override.combat_modifier = 0
override.movement_modifier = 0
override.environment_chance = 0
override.force_crt = 0
